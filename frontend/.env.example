# Backend API URL (required when frontend & backend are on SEPARATE Vercel projects)
# Set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend-project.vercel.app
#
# Leave blank (or remove) if both are on the SAME Vercel project with rewrites.
VITE_API_URL=
